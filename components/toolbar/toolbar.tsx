import { RotateCcw, Undo2, Redo2, Hexagon } from 'lucide-react';
import type { ReactNode } from 'react';
export function Toolbar({ name, onName, undo, redo, canUndo, canRedo, reset, children }: { name: string; onName: (name: string) => void; undo: () => void; redo: () => void; canUndo: boolean; canRedo: boolean; reset: () => void; children?: ReactNode }) {
  return <header className="toolbar"><div className="brand" aria-label="ValarMurgulis STUDIO"><span className="brand-icon"><Hexagon size={23} strokeWidth={2.5} /></span><span>ValarMurgulis<span className="brand-studio">STUDIO</span></span></div><div className="toolbar-divider" /><input className="project-name" aria-label="Project name" value={name} maxLength={100} onChange={(e) => onName(e.target.value)} /><div className="toolbar-actions"><button className="icon-button" title="Undo" aria-label="Undo" disabled={!canUndo} onClick={undo}><Undo2 size={17} /></button><button className="icon-button" title="Redo" aria-label="Redo" disabled={!canRedo} onClick={redo}><Redo2 size={17} /></button><button className="icon-button" title="Reset project" aria-label="Reset project" onClick={reset}><RotateCcw size={16} /></button>{children}</div></header>;
}

