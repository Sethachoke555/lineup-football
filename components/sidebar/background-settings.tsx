import type { Background } from '@/types/project';
import { Range, Select } from './fields';
import { ImageUpload } from '@/components/players/image-upload';
export function BackgroundSettings({ background, onChange, onError }: { background: Background; onChange: (patch: Partial<Background>) => void; onError: (message: string) => void }) {
  return <div className="panel-fields"><div className="section-heading"><h2>Background</h2><span>05</span></div><Select label="Scene" value={background.kind} onChange={(kind) => onChange({ kind: kind as Background['kind'] })}><option value="pitch">Football pitch</option><option value="stadium">Stadium under lights</option><option value="gradient">Gradient</option><option value="custom">Custom image</option></Select>{background.kind === 'custom' && <><ImageUpload label="Custom background" value={background.image} onChange={(image) => onChange({ image })} onError={onError} />{!background.image && <p className="muted">Upload an image to replace the gradient.</p>}</>}<div className="inspector-section"><h3>Scene adjustments</h3><Range label="Brightness" value={background.brightness} min={20} max={160} onChange={(brightness) => onChange({ brightness })} unit="%" /><Range label="Blur" value={background.blur} min={0} max={20} onChange={(blur) => onChange({ blur })} unit="px" /><Range label="Dark overlay" value={background.overlay} min={0} max={90} onChange={(overlay) => onChange({ overlay })} unit="%" /></div><div className="tip-card"><strong>Let your players stand out.</strong><p>A darker backdrop and a little blur keep attention on your starting eleven.</p></div></div>;
}
